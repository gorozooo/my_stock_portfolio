// 入力欄のEnter押下でフォーム送信などの拡張用
document.addEventListener("DOMContentLoaded", function() {
    const form = document.querySelector(".login-form");
    form.addEventListener("submit", function(e){
        // ここで必要な追加処理を書く
        // 例: ボタンを押したらアニメーションを出すなど
    });
});
